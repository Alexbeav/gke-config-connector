apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

metadata:
  name: security-baseline

# Security policies for the platform
resources:
  - namespace.yaml
  - gatekeeper-policies.yaml

# Namespace for security policies
namespace: security-baseline
