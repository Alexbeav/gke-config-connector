apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

metadata:
  name: monitoring-stack

# Placeholder monitoring stack
# To be expanded with Prometheus, Grafana, etc.

resources: []

# Namespace for monitoring
namespace: monitoring

# Create namespace
resources:
  - namespace.yaml
